# Learnings

## Visual Consistency
- Refactored `IntelligenceSummary.tsx` to match `OutcomesAnalysisTable.tsx`.
- Used consistent container styles: `rounded-md border border-white/[0.08] bg-white/[0.02] shadow-[0_0_0_1px_rgba(0,0,0,1)]`.
- Used consistent header styles: `h-10 border-b border-white/[0.08] flex items-center px-4 bg-transparent` with uppercase tracking text.
- Moved metadata to a subtle footer to reduce clutter.
- Removed accordion interaction for a cleaner, always-visible layout.
